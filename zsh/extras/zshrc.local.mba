#!/usr/bin/env zsh
alias gpg=gpg2

alias emacsclient="emacsclient -q -a emacs -n"
alias e=emacsclient
alias pstree="pstree -g 3"

if [ -f ~/.colors.sh ]; then
    ~/.colors.sh
fi

unalias run-help
autoload run-help
HELPDIR=/usr/local/share/zsh/help
