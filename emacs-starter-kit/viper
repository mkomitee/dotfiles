(setq viper-inhibit-startup-message 't)
(setq viper-expert-level '1)

(setq viper-case-fold-search t) ; don't touch or else...

(setq viper-re-search t) ; don't touch or else...

(setq blink-matching-paren t) ; don't touch or else...

