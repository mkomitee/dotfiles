set spell         " spell check ftw
set tabstop=2     " 2 space tabs
set shiftwidth=2  " indentation uses 2 spaces as well e.g. >>
set expandtab     " uses spaces, not tabs
set softtabstop=2 " use 2 spaces when tabbing because, ...
