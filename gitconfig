[user]
    name = Michael Komitee

[color]
    ui = auto

[core] 
    pager = less -FRSX 
    abbrev=8
    editor = vim
    whitespace=fix,-indent-with-non-tab,trailing-space,cr-at-eol 
    excludesfile = ~/.dotfiles/gitignore

[log]
    abbrevcommit = yes
    decorate = short
    showroot = true
    date=short

[interactive]
    single = yes

[pretty]
    lg = "%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset"

[alias] 
    lg = log --graph --pretty=lg --abbrev-commit 
    lc = local-commits --decorate=short --pretty=lg
    graph = log -w --graph --full-history --all --pretty=lg --decorate=short
    track = checkout -t
    ls = ls-files
    rl = reflog
    branches = branch -av
    d = diff --stat --summary
    ds = diff --staged
    ref = show-ref
    vimdiff = difftool
    head = log -1
    stat = status -sb
    st = status -sb
    ci = commit
    co = checkout 
    di = diff
    dc = diff --cached
    aa = add --all
    sm = submodule
    wc = whatchanged
    aa = add -A
    commit = commit -v
    ammend = commit --amend -v
    amend = commit --amend -v
    tags = tag -ln1
    remotes = remote -v
    ctags = !.git/hooks/ctags
    reinit = !.git/hooks/reinit

[pager]
    status = yes
    show-branch = yes

[clean]
    requireForce = no

[url "gitserve"]
    insteadOf = gs

[difftool "vimdiff"]
    cmd = vimdiff  \"$LOCAL\" \"$REMOTE\"

[format]
    pretty = lg

[diff]
    tool = vimdiff
    guitool = gvimdiff

[branch]
    autosetuprebase = always

[github]
    user = mkomitee

[url "git@github.com:"]
    insteadOf = "gh:"
    pushInsteadOf = "github:"
    pushInsteadOf = "git://github.com/"

[tig]
    show-author = no
    show-date = no
    show-line-numbers = yes
    show-rev-graph = yes

[mergetool "vmt"]
    cmd = vim -f -d -c \"wincmd J\" \"$MERGED\" \"$LOCAL\" \"$BASE\" \"$REMOTE\"

[mergetool "gvmt"]
    cmd = gvim -f -d -c \"wincmd J\" \"$MERGED\" \"$LOCAL\" \"$BASE\" \"$REMOTE\"

[mergetool "splice"]
    cmd = "vim -f $BASE $LOCAL $REMOTE $MERGED -c 'SpliceInit'"
    trustExitCode = true

[merge]
    tool = splice

[push]
    default = upstream
